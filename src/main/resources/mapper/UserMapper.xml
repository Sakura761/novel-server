<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.novel.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.sakura.novel.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="status" property="status" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password_hash, nickname, email, avatar_url, status, create_time, update_time
    </sql>

    <select id="selectById" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        ORDER BY create_time DESC
    </select>

    <select id="selectByPage" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countTotal" resultType="int">
        SELECT COUNT(*)
        FROM users
    </select>

    <insert id="insert" parameterType="com.sakura.novel.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, nickname, email, avatar_url, status, create_time, update_time)
        VALUES (#{username}, #{passwordHash}, #{nickname}, #{email}, #{avatarUrl}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById">
        UPDATE users
        SET username = #{username},
            password_hash = #{passwordHash},
            nickname = #{nickname},
            email = #{email},
            avatar_url = #{avatarUrl},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

</mapper>
